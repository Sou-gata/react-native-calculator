import { View, Text } from "react-native";
import React from "react";

const Volume = () => {
    return (
        <View>
            <Text>Volume</Text>
        </View>
    );
};

export default Volume;
